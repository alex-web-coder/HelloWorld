import { NoteShortenerPipe } from './note-shortener-pipe';

describe('NoteShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
